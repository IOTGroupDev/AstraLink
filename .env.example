# ========================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and fill with your values
# These variables are used by docker-compose.yml

# ========================================
# POSTGRESQL DATABASE
# ========================================
POSTGRES_DB=astralink
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_db_password_here

# ========================================
# SECURITY
# ========================================
# Generate strong secret: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# ========================================
# BACKEND CONFIGURATION
# ========================================
BACKEND_PORT=3000
REDIS_URL=redis://redis:6379

# Database URL for backend container
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public

# ========================================
# AI PROVIDERS (Optional for docker-compose)
# ========================================
# Set these if you want to configure AI providers via docker-compose
# Otherwise, configure them in backend/.env
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# OPENAI_API_KEY=sk-proj-your-openai-key-here
