# AstraLink — Запуск Expo Go на iPhone с доступом к локальному backend через HTTPS‑туннель

Этот гайд устраняет «Network Error» при работе Expo Go на iPhone, когда фронтенд должен обращаться к локальному backend. На iOS требуется HTTPS (ATS), поэтому мы поднимаем публичный HTTPS‑туннель к backend и указываем этот URL во фронтенде.

Ссылки на файлы:

- ENV фронтенда: [frontend/.env](frontend/.env)
- Шаблон ENV: [frontend/.env.example](frontend/.env.example)
- Скрипты npm: [frontend/package.json](frontend/package.json)
- Настройка CORS/сервер: [backend/src/main.ts](backend/src/main.ts:99)
- Выбор Base URL в коде: [getApiBaseUrl()](frontend/src/services/api.ts:29)

## 1) Запустить backend локально

- Backend слушает на всех интерфейсах (0.0.0.0) и имеет префикс /api (см. [main.ts](backend/src/main.ts:123)):
  - Порт по умолчанию: 3000
  - Эндпоинт здоровья: /api/health

Пример:

```bash
cd backend
npm run start:dev
```

Убедитесь, что http://localhost:3000/api/health отвечает OK.

## 2) Поднять публичный HTTPS‑туннель к backend

Рекомендуемый вариант: Cloudflare quick tunnel

```bash
cloudflared tunnel --url http://127.0.0.1:3000
```

Команда выведет домен вида:

```
https://SOMETHING.trycloudflare.com
```

Альтернатива: localtunnel (если cloudflared недоступен)

```bash
npx localtunnel --port 3000 --subdomain your-subdomain
# Итог: https://your-subdomain.loca.lt
```

Важно:

- Backend остаётся доступным локально, но фронту на iPhone нужен публичный HTTPS‑домен туннеля.
- Туннель должен быть активен всё время работы приложения.

## 3) Прописать домен туннеля во фронтенд ENV

Откройте [frontend/.env](frontend/.env) и установите:

```
EXPO_PUBLIC_API_URL=https://YOUR_TUNNEL_DOMAIN/api
```

Примеры:

```
EXPO_PUBLIC_API_URL=https://federal-indication-genuine-fighters.trycloudflare.com/api
# или для localtunnel:
# EXPO_PUBLIC_API_URL=https://your-subdomain.loca.lt/api
```

Подсказка: есть шаблон [frontend/.env.example](frontend/.env.example) — можно скопировать в .env.

## 4) Запустить Expo Dev Server в режиме tunnel

Мы уже добавили удобные скрипты в [package.json](frontend/package.json:5):

```json
{
  "scripts": {
    "start": "expo start",
    "start:reset": "expo start -c",
    "start:lan": "expo start --lan",
    "tunnel": "expo start --tunnel",
    "tunnel:ci": "CI=1 EXPO_NO_INTERACTIVE=1 expo start --tunnel --port 8082"
  }
}
```

Рекомендуемый запуск для CI/неинтерактивного окружения:

```bash
cd frontend
npm run start:reset
npm run tunnel:ci
```

Зачем:

- `start:reset` очищает кэш Metro, чтобы подтянулся свежий EXPO_PUBLIC_API_URL.
- `tunnel:ci` запускает Expo в режиме tunnel без вопросов/подтверждений и на порту 8082 (если 8081 занят).

Если при первом запуске Expo попросит установить @expo/ngrok для tunnel — он уже добавлен в devDependencies, но если потребуется:

```bash
cd frontend
npm i -D @expo/ngrok@^4.1.0
```

Примечания:

- Если видите «Port 8081 is running this app in another window», используйте порт 8082 (скрипт `tunnel:ci` уже делает это).
- Не используйте глобальную установку @expo/ngrok без sudo — могут быть EACCES. Локальная dev-зависимость безопаснее.

## 5) Подключить iPhone и проверить

- Откройте Expo Go на iPhone.
- Сканируйте QR‑код из терминала Expo Dev Server или откройте ссылку вручную.
- В логах фронтенда должна появиться строка из [getApiBaseUrl()](frontend/src/services/api.ts:124):
  ```
  🌐 API Base URL: https://YOUR_TUNNEL_DOMAIN/api
  ```
- В Safari на iPhone проверьте:
  ```
  https://YOUR_TUNNEL_DOMAIN/api/health
  ```
  Должно быть OK.

Если всё корректно, повторите OTP-вход/авторизацию. Ошибка «Network Error» на /user/profile и /auth/complete-signup должна исчезнуть.

## Важные детали и объяснения

- Почему HTTPS: iOS/ATS блокирует небезопасные HTTP‑запросы для приложений без специальных исключений. Поэтому для iPhone используйте публичный HTTPS‑туннель.
- CORS: Для нативного RN CORS обычно не мешает (запросы без Origin). В backend CORS настроен рефлективно (см. [main.ts](backend/src/main.ts:99)). Для веба используйте тот же домен/порт или корректный origin.
- API префикс: Backend публикует API под /api, не забудьте добавить /api в EXPO_PUBLIC_API_URL.
- Смена ENV: Любое изменение .env требует перезапуска Expo (желательно с `-c`).

## Тест‑чеклист

1. Backend: http://localhost:3000/api/health — OK
2. Туннель: https://YOUR_TUNNEL_DOMAIN/api/health — OK (снаружи, включая iPhone)
3. Expo Dev Server: запущен (tunnel), QR‑код отображается
4. На iPhone: Expo Go открывает приложение, в логах:
   ```
   🌐 API Base URL: https://YOUR_TUNNEL_DOMAIN/api
   ```
5. Повтор входа/регистрации: запросы к /user/profile и /auth/complete-signup успешны (нет ERR_NETWORK)

## Траблшутинг

- В Expo спрашивает «The package @expo/ngrok is required…» — установите dev‑зависимость:
  ```bash
  cd frontend
  npm i -D @expo/ngrok@^4.1.0
  ```
- «Port 8081 is running…» — используйте `npm run tunnel:ci` (порт 8082) или явно `--port 8082`.
- Health открывается в Safari, но фронт видит «Network Error»:
  - Проверьте, что в логах фронта Base URL совпадает с туннелем.
  - Перезапустите Expo с `npm run start:reset`.
  - Убедитесь, что токен Supabase прикладывается (см. логи интерцептора в [api.ts](frontend/src/services/api.ts:137)).
- LAN вариант (только если одно Wi‑Fi и не требуется HTTPS):
  ```
  EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:3000/api
  ```
  В таком случае Expo Dev Server можно запустить с `npm run start:lan`. На iOS http часто блокируется — предпочитайте HTTPS‑туннель.

## Финализация конфигурации

- Зафиксируйте актуальный домен в [frontend/.env](frontend/.env) или используйте .env.local на машине разработчика.
- Сохраните этот файл (README_Tunnel.md) в репозитории как инструкцию для команды.
- При деплое используйте продакшн‑домен API и не включайте tunnel.
